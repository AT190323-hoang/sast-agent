# Project instructions for Claude Code sessions — kept local only, not pushed to remote.
CLAUDE.md

# Tài liệu nội bộ công ty (đề bài + rubric) — không public, chỉ dùng local.
docs/lo-trinh-thuc-tap-6-tuan.pdf
